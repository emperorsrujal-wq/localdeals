{
  "name": "blog-automation",
  "version": "2.0.0",
  "description": "Automated daily blog post generator using Google Gemini API",
  "scripts": {
    "generate": "ts-node generateDailyBlog-gemini.ts",
    "start": "node dist/generateDailyBlog-gemini.js",
    "build": "tsc",
    "test": "ts-node generateDailyBlog-gemini.ts"
  },
  "dependencies": {
    "@google/generative-ai": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
